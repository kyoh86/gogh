mutation CreateRepository($name: String!, $visibility: RepositoryVisibility = PUBLIC, $ownerId:ID) {
  createRepository(input:{
    name: $name,
    visibility: $visibility,
    ownerId: $ownerId,
  }) {
    repository{
      name
      url
    }
  }
}

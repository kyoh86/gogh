query ListRepoUrls($repositoryCursor: String) { 
  viewer { 
    repositories(first:30, after:$repositoryCursor) {
      edges {
        node {
          url
        }
        cursor
      }
    }
  }
}

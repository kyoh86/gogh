query ListMyOrganizations($organizationCursor: String) {
  viewer {
    organizations(after: $organizationCursor, first: 100) {
      edges {
        node {
          login
          id
        }
        cursor
      }
    }
  }
}
